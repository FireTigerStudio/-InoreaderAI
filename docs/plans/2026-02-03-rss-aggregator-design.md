# RSS智能聚合与分析系统 - 设计文档

> 日期: 2026-02-03
> 状态: 待实现
> 版本: MVP v1.0

---

## 一、项目概述

基于 Inoreader RSS 订阅源，构建自动化新闻抓取、AI 分析、智能分类系统。系统每日处理 200-300 篇文章，Urgent 类文章即时推送微信，所有文章写入当日 Excel 文件，通过网页展示和下载。

### 核心价值
- **自动化**: GitHub Actions 定时运行，零人工干预
- **智能分析**: Gemini AI 一句话摘要 + 重要性评分
- **即时推送**: Urgent 文章 2 小时内推送微信
- **归档查阅**: 每日 Excel 文件，网页一键下载

---

## 二、设计决策汇总

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 运行环境 | GitHub Actions | 免费 2000 分钟/月，零服务器成本 |
| Excel 存储 | GitHub 仓库 | 自带版本历史，无额外费用 |
| 前端技术 | 静态 HTML + TailwindCSS | MVP 最快，无需构建步骤 |
| API 限流 | 请求间隔 3 秒 | 简单可靠，月成本约 $4.5 |
| 微信推送 | Server酱付费版 | 无限制推送，约 ¥40/年 |
| 查重方式 | URL 去重 | 读 Excel 检查，跨天自动重置 |
| Tag 管理 | 网页 UI + GitHub API | 可视化管理，自动触发部署 |
| AI 输出 | 一句话摘要 + 评分 | Token 消耗最少，成本最低 |

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                      GitHub Actions                           │
│                                                               │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                │
│  │ 定时触发  │───▶│ 抓取脚本  │───▶│ 读当日    │                │
│  │ 2h/12h   │    │          │    │ Excel去重 │                │
│  └──────────┘    └──────────┘    └──────────┘                │
│                                        │                      │
│                                        ▼ (新文章)             │
│                                  ┌──────────┐                │
│                                  │ Gemini   │                │
│                                  │ AI分析   │                │
│                                  │ 每篇都做  │                │
│                                  └──────────┘                │
│                                        │                      │
│                         ┌──────────────┴──────────────┐      │
│                         ▼                              ▼      │
│                   ┌──────────┐                  ┌──────────┐ │
│                   │ Urgent?  │───Yes──────────▶│ 微信推送  │ │
│                   └──────────┘                  └──────────┘ │
│                         │                              │      │
│                         ▼ (全部文章)                   │      │
│                   ┌──────────┐◀────────────────────────┘      │
│                   │ 写入Excel │                               │
│                   │ Git提交   │                               │
│                   └──────────┘                               │
└──────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                    GitHub Pages                               │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐       │
│  │  首页       │    │  Tag管理    │    │  Excel文件  │       │
│  │  统计+下载  │    │  增删改     │    │  /data/     │       │
│  └─────────────┘    └─────────────┘    └─────────────┘       │
└──────────────────────────────────────────────────────────────┘
```

### 核心流程
1. Actions 定时触发 → 读取 `config/tags.json`
2. 遍历每个 Tag → 抓取 Inoreader JSON → 解析文章
3. 对比当日 Excel 去重 → 新文章送 Gemini 分析
4. 判断 Tag 类型：Urgent → 额外推送微信
5. 所有文章都写入 Excel
6. Commit Excel 到 `/data/` 目录

---

## 四、目录结构

```
InoreaderAI/
├── .github/
│   └── workflows/
│       ├── fetch-urgent.yml      # 每2小时运行
│       └── fetch-normal.yml      # 每12小时运行
│
├── config/
│   └── tags.json                 # Tag配置（网页可编辑）
│
├── scripts/
│   ├── main.js                   # 主入口
│   ├── fetch.js                  # 抓取Inoreader
│   ├── analyze.js                # 调用Gemini
│   ├── excel.js                  # 读写Excel
│   └── wechat.js                 # Server酱推送
│
├── data/
│   ├── news_2026-02-03.xlsx      # 今日数据
│   ├── news_2026-02-02.xlsx      # 昨日数据
│   └── ...                       # 保留7天
│
├── web/
│   ├── index.html                # 首页（统计+下载）
│   ├── tags.html                 # Tag管理页
│   ├── styles.css                # TailwindCSS
│   └── app.js                    # 前端逻辑
│
├── docs/
│   └── plans/
│       └── 2026-02-03-rss-aggregator-design.md
│
├── CLAUDE.md                     # 项目手册（Architect生成）
└── package.json                  # Node.js依赖
```

---

## 五、数据结构

### 5.1 tags.json 配置格式

```json
{
  "tags": [
    {
      "name": "P0-Urgent",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/P0-Urgent/view/json",
      "type": "urgent"
    },
    {
      "name": "Cyprus-URGENT",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/Cyprus-URGENT/view/json",
      "type": "urgent"
    },
    {
      "name": "金银矿产业链",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/%E9%87%91%E9%93%B6%E7%9F%BF%E4%BA%A7%E4%B8%9A%E9%93%BE/view/json",
      "type": "normal"
    },
    {
      "name": "黄金-美元-利率",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/%E9%BB%84%E9%87%91-%E7%BE%8E%E5%85%83-%E5%88%A9%E7%8E%87/view/json",
      "type": "normal"
    },
    {
      "name": "macro-中国",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/MacroEco-%E4%B8%AD%E5%9B%BD/view/json",
      "type": "normal"
    },
    {
      "name": "macro-美国",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/MacroEco-%E7%BE%8E%E5%9B%BD/view/json",
      "type": "normal"
    },
    {
      "name": "macro-欧洲",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/MacroEco-%E6%AC%A7%E6%B4%B2/view/json",
      "type": "normal"
    },
    {
      "name": "tech",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/tech/view/json",
      "type": "normal"
    },
    {
      "name": "国家紧急",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/%E5%9B%BD%E5%AE%B6%E7%B4%A7%E6%80%A5/view/json",
      "type": "normal"
    },
    {
      "name": "大宗商品波动",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/1-%E5%A4%A7%E5%AE%97%E5%95%86%E5%93%81%E6%B3%A2%E5%8A%A8/view/json",
      "type": "normal"
    },
    {
      "name": "市场异常",
      "url": "https://www.inoreader.com/stream/user/1003496420/tag/%E5%B8%82%E5%9C%BA%E5%BC%82%E5%B8%B8/view/json",
      "type": "normal"
    }
  ]
}
```

### 5.2 Excel 字段结构

| 列名 | 类型 | 说明 |
|------|------|------|
| Tag | string | 来源标签名 |
| URL | string | 原文链接（用于去重） |
| Title | string | 文章标题 |
| Summary | string | AI生成的一句话摘要（30字内） |
| Score | number | AI重要性评分 1-5 |
| Type | string | "urgent" 或 "normal" |
| Date | datetime | 抓取时间戳 |

### 5.3 微信推送格式

```
🚨 紧急新闻 [Tag名]

📰 标题1 (⭐5)
摘要内容...
🔗 链接

📰 标题2 (⭐4)
摘要内容...
🔗 链接
```

---

## 六、GitHub Actions 工作流

### 6.1 fetch-urgent.yml (每2小时)

```yaml
name: Fetch Urgent
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: node scripts/main.js --type urgent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          WECHAT_KEY: ${{ secrets.WECHAT_KEY }}
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📰 Urgent update"
          file_pattern: "data/*.xlsx"
```

### 6.2 fetch-normal.yml (每12小时)

```yaml
name: Fetch Normal
on:
  schedule:
    - cron: '0 8,20 * * *'
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: node scripts/main.js --type normal
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          WECHAT_KEY: ${{ secrets.WECHAT_KEY }}
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📰 Normal update"
          file_pattern: "data/*.xlsx"
```

---

## 七、网页界面设计

### 7.1 首页 (index.html)

```
┌─────────────────────────────────────────────────────────────┐
│  RSS智能聚合系统                              [Tag管理]      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │        📥 下载今日新闻 (2026-02-03)                  │   │
│  │              news_2026-02-03.xlsx                   │   │
│  │                  [立即下载]                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐  │
│  │ 今日文章   │ │ Urgent    │ │ Normal    │ │ 最后更新   │  │
│  │    127    │ │    23     │ │   104     │ │  14:30    │  │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘  │
│                                                             │
│  历史文件                                                   │
│  ├─ news_2026-02-02.xlsx  [下载]                           │
│  ├─ news_2026-02-01.xlsx  [下载]                           │
│  └─ ...最近7天                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 Tag管理页 (tags.html)

```
┌─────────────────────────────────────────────────────────────┐
│  Tag管理                                      [← 返回首页]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [+ 添加新Tag]                                              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🔴 P0-Urgent                              [urgent ▼]│   │
│  │ URL: https://www.inoreader.com/stream/...           │   │
│  │                                    [编辑] [删除]     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🟢 金银矿产业链                           [normal ▼]│   │
│  │ URL: https://www.inoreader.com/stream/...           │   │
│  │                                    [编辑] [删除]     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ GitHub Token: ●●●●●●●●●●●●          [修改Token]      │   │
│  │ 首次使用需设置，用于保存配置到GitHub                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 设计风格
- 参考: 苹果官网商务风格
- 背景: 米白色 (#F5F5F7)
- 卡片: 白色圆角卡片
- 强调色: 高饱和度时尚色（红/金/橙/紫）
- 响应式: 适配移动端

---

## 八、核心脚本逻辑

### 8.1 main.js 主流程

```javascript
async function main(type) {  // type = 'urgent' | 'normal'
  // 1. 读取配置
  const tags = loadTags().filter(t => t.type === type)

  // 2. 读取今日Excel（用于去重）
  const todayFile = `data/news_${getToday()}.xlsx`
  const existingUrls = getExistingUrls(todayFile)

  // 3. 遍历每个Tag
  const allArticles = []
  for (const tag of tags) {
    const articles = await fetchInoreader(tag.url, 10)  // 每次10篇
    const newArticles = articles.filter(a => !existingUrls.has(a.url))
    allArticles.push(...newArticles.map(a => ({ ...a, tag })))
  }

  // 4. AI分析（带限流）
  for (const article of allArticles) {
    await sleep(3000)  // 3秒间隔
    const { summary, score } = await analyzeWithGemini(article)
    article.summary = summary
    article.score = score
  }

  // 5. 推送Urgent到微信
  const urgentArticles = allArticles.filter(a => a.tag.type === 'urgent')
  if (urgentArticles.length > 0) {
    await pushToWechat(urgentArticles)
  }

  // 6. 全部写入Excel
  appendToExcel(todayFile, allArticles)

  // 7. 清理7天前的旧文件
  cleanOldFiles('data/', 7)
}
```

### 8.2 Gemini Prompt

```javascript
const prompt = `你是新闻分析师。用一句话（30字内）总结这篇新闻，并评估重要性。

标题：${article.title}
来源：${article.source}

返回JSON格式：{"summary":"一句话摘要","score":3}
score范围1-5，5最重要。只返回JSON，不要其他内容。`
```

---

## 九、错误处理策略

| 场景 | 处理方式 |
|------|----------|
| Inoreader抓取失败 | 跳过该Tag，继续其他Tag，记录日志 |
| Gemini API失败 | 重试1次，仍失败则summary写"分析失败"，score写0 |
| Gemini返回格式错误 | 尝试提取文本，失败则同上 |
| Excel文件不存在 | 自动创建新文件（每日首次运行） |
| 微信推送失败 | 记录日志，不影响Excel写入 |
| GitHub提交失败 | Actions自动重试机制处理 |

### 日志输出格式

```
[2026-02-03 14:30] Fetch Urgent 完成
- 抓取: 3个Tag, 共25篇
- 去重后: 18篇新文章
- AI分析: 18篇成功, 0篇失败
- 微信推送: 1条 (包含8篇urgent)
- Excel写入: 18条
```

---

## 十、Subagent 分工

### 10.1 开发流程图

```
Phase 1: 需求与架构
┌──────────────┐    ┌──────────────┐
│ Product      │───▶│ Architect    │
│ Manager ✅   │    │ 生成CLAUDE.md │
│ (已完成)     │    │              │
└──────────────┘    └──────────────┘
                           │
Phase 2: 集成外部服务       ▼
                    ┌──────────────┐
                    │ Integration  │
                    │ Specialist   │
                    │ ├─ Inoreader │
                    │ ├─ Gemini    │
                    │ ├─ Server酱  │
                    │ └─ GitHub API│
                    └──────────────┘
                           │
Phase 3: 核心开发           ▼
          ┌────────────────┴────────────────┐
          ▼                                  ▼
   ┌──────────────┐                  ┌──────────────┐
   │ Backend      │                  │ Frontend     │
   │ Developer    │                  │ Developer    │
   │ ├─ main.js   │                  │ ├─ index.html│
   │ ├─ fetch.js  │                  │ ├─ tags.html │
   │ ├─ analyze.js│                  │ └─ app.js    │
   │ ├─ excel.js  │                  │              │
   │ └─ wechat.js │                  │              │
   └──────────────┘                  └──────────────┘
          │                                  │
          └────────────────┬─────────────────┘
Phase 4: 测试               ▼
                    ┌──────────────┐
                    │ Test         │
                    │ Engineer     │
                    │ 单元测试+集成 │
                    └──────────────┘
                           │
Phase 5: 审查               ▼
                    ┌──────────────┐
                    │ Code         │
                    │ Reviewer     │
                    │ 安全+质量检查 │
                    └──────────────┘
                           │
                           ▼
                       🚀 部署
```

### 10.2 详细任务分配

| Subagent | 负责模块 | 具体任务 |
|----------|----------|----------|
| **Architect** | 架构设计 | 生成 CLAUDE.md 项目手册，定义技术栈和开发规范 |
| **Integration Specialist** | 外部API | 封装 Inoreader/Gemini/Server酱/GitHub API，处理认证和错误 |
| **Backend Developer** | 核心脚本 | main.js, fetch.js, analyze.js, excel.js, wechat.js, GitHub Actions YAML |
| **Frontend Developer** | 网页界面 | index.html, tags.html, app.js, TailwindCSS 响应式设计 |
| **Test Engineer** | 测试 | 单元测试、集成测试、模拟 API 测试 |
| **Code Reviewer** | 审查 | 安全检查（无泄露密钥）、代码质量、部署前审批 |

---

## 十一、MVP 实现清单

### 11.1 需要实现的文件

| 文件 | 功能 | 负责 Agent |
|------|------|------------|
| `scripts/main.js` | 主入口，协调所有流程 | Backend Developer |
| `scripts/fetch.js` | 抓取 Inoreader JSON | Integration Specialist |
| `scripts/analyze.js` | 调用 Gemini API | Integration Specialist |
| `scripts/excel.js` | 读写 Excel 文件 | Backend Developer |
| `scripts/wechat.js` | Server酱推送 | Integration Specialist |
| `config/tags.json` | 11个 Tag 初始配置 | Architect |
| `.github/workflows/fetch-urgent.yml` | 2小时定时任务 | Backend Developer |
| `.github/workflows/fetch-normal.yml` | 12小时定时任务 | Backend Developer |
| `web/index.html` | 首页下载+统计 | Frontend Developer |
| `web/tags.html` | Tag管理页 | Frontend Developer |
| `web/app.js` | 前端交互逻辑 | Frontend Developer |
| `CLAUDE.md` | 项目手册 | Architect |

### 11.2 需要配置的 Secrets

| Secret 名 | 来源 | 用途 |
|-----------|------|------|
| `GEMINI_API_KEY` | Google AI Studio | AI 分析 |
| `WECHAT_KEY` | Server酱后台 | 微信推送 |

### 11.3 前端需要用户设置

| 配置项 | 存储位置 | 用途 |
|--------|----------|------|
| GitHub Personal Access Token | localStorage | 调用 GitHub API 保存 Tag 配置 |

---

## 十二、约束条件

| 约束 | 策略 |
|------|------|
| **成本控制** | Gemini API 限流 3 秒/请求，月预算 $50 |
| **时效性** | Urgent 2h 内推送（GitHub Actions cron） |
| **部署简化** | GitHub Pages 静态托管，零服务器 |
| **数据量** | 200-300 条/日，Excel 单文件 < 100KB |
| **保留期限** | 自动清理 7 天前的 Excel 文件 |

---

## 十三、下一步行动

1. **Architect**: 生成 `CLAUDE.md` 项目手册
2. **Integration Specialist**: 封装四个外部 API
3. **Backend Developer**: 实现核心脚本
4. **Frontend Developer**: 构建网页界面
5. **Test Engineer**: 编写测试用例
6. **Code Reviewer**: 审查并批准部署

---

*文档生成时间: 2026-02-03*
*状态: 待 Architect 生成 CLAUDE.md 后开始实现*
